type User @model {
  id: ID!
  email: String
  todos: [Todo] @connection(name: "UserTodos")
}

type Todo @model {
  id: ID!
  description: String
  status: TodoStatus
  # createdAt: new Date().toISOString()
  owner: User! @connection(name: "UserTodos")
}

enum TodoStatus {
  done
  pending
}
